{
  "dependencies": {
    "gitbook-cli": "^2.3.2"
  },
  "scripts": {
    "init": "gitbook init",
    "prebuild": "rimraf _book && gitbook install",
    "build": "gitbook build"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  },
  "engines": {
    "node": "10.23.0"
  }
}
